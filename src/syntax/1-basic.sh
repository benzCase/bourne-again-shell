# Here, we declare the shell we are going to use.
#!/bin/bash

# Basic Syntax, printing is 'echoing'
echo "Hello, World!"
echo "I am learning BASH"

# Note that the 'echo' function already puts \n character at the end.

# Obviously, the comments start with #

# The order of the execution is from TOP to BOTTOM.

# Finally, we can use the semicolons ';' to run a lot of commands on the same line
echo "Running a lot "; echo "Of commmands"; echo "On the same line"
